/target
**/*.rs.bk
junit.xml
report.xml
.DS_Store
.env
__pycache__/
*.pyc

# Example cassettes are recorded with contributors' own API keys; committing
# one is a deliberate act, not a default.
examples/**/.evalcore/
site/demo/**/.evalcore/

# Local editor/vault state
.obsidian/

# Private knowledge base (its own repo, pushed to eval-core/brain) and the raw
# PRD export it was seeded from. Product-internal material never enters this
# repo's history.
/wiki/
/prd.zip

# Local learning sandbox (not tracked)
playground/

# Playwright MCP session artifacts (screenshots, snapshots, console logs)
.playwright-mcp/
