---
title: Know when your AI gets worse, before your users do
description: "EvalCore is snapshot testing for AI behavior. Record how your LLM app or agent behaves, then replay it in CI on every change: offline, deterministic, $0."
template: splash
hero:
  tagline: Change a prompt, swap a model, update a dependency. Did anything break? EvalCore records how your AI behaves and checks every change against it. One small binary, any language, no flaky tests.
  actions:
    - text: Get started
      link: getting-started/quickstart/
      variant: primary
    - text: Install
      link: getting-started/installation/
      variant: secondary
---

import HowItWorks from '../../components/landing/HowItWorks.astro';
import FeatureExplorer from '../../components/landing/FeatureExplorer.tsx';
import Cta from '../../components/landing/Cta.astro';

<HowItWorks />

<section class="ec-features" id="features">
	<header class="ec-features-head">
		<h2>Everything a suite needs, one binary</h2>
		<p>Pick a capability. Each one is a few lines of YAML or a flag, not a framework.</p>
	</header>
	<FeatureExplorer client:visible />
</section>

<p class="ec-stack"><span class="ec-stack-label">Works with</span> OpenAI, vLLM, Ollama, any OpenAI-compatible gateway, your own REST APIs, shell commands, and OTel / OpenInference traces.</p>

<Cta />
