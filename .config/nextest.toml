# cargo-nextest configuration — https://nexte.st/docs/configuration/
[profile.default]
retries = 0
failure-output = "immediate"

# `cargo nextest run --profile ci` in GitHub Actions
[profile.ci]
retries = 0
failure-output = "immediate-final"
fail-fast = false

[profile.ci.junit]
path = "junit.xml"
